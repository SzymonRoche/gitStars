const userNames = () => {
    // All users that you need to display. 
    return ['yyx990803', 'toddmotto', 'johnpapa', 'angular', 'facebook', 'vuejs']
};

let UsersNamesArray = []

  u.unshift(...userNames())

  console.log(tab[1])

export default () => {
    userNames
};

