import  userNames  from "./users.js";
import tabela from "./tabele.js";

function CreateTabele(left, mid, right) {
    const tab = document.querySelector(".my-table");

    const tabela = tab.insertRow(0);
    const tabela2 = tab.insertRow(0);

    this.left = this.tabela.insertCell(0);
    this.mid = this.tabela.insertCell(1);
    this.right = this.tabela.insertCell(2);


    this.left = this.innerHTML = "USERNAME"
    topMiddle this.innerHTML = "COUNTS"
    TopRight =this.innerHTML = "STARS"



};

CreateTabele();


console.log(tabela);